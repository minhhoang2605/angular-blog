import { ID } from '@datorama/akita';
import { Post } from '../app/post/state/post.model';

export class MockPostService {
  constructor( ) {}

  async add(post: Post) {
    
  }

  async update(id, post: Partial<Post>) {
    
  }

  async remove(id: ID) {
    
  }
}
